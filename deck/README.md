# TRAE K2 介绍幻灯片

拍视频请打开 **`TRAE-K2-real.pptx`**（实物照片版）。每页备注里是口播稿，也可对照 `../docs/video-script.md`。

若 `TRAE-K2.pptx` 正被 PowerPoint 占用，脚本会写到 `TRAE-K2-real.pptx`。

重新生成：

```bat
cd deck
npm install
node build-deck.js
```

字体：微软雅黑。深色，和工牌屏一致。
